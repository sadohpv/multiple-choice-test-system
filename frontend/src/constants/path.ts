export const AUTH_PATHS = {
    login: "/login",
    register: "/register",
} as const;

export const APP_PATHS = {
    home: "/",
    practice: "practice",
    history: "history",
    results: "results",
    contribute: "contribute",
    profile: "profile",
} as const;
